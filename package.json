{
    "name": "est",
    "version": "1.0.0",
    "private": true,
    "description": "Est - Self-hostable planning poker application",
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
        "dev:server": "npm run dev --workspace=server",
        "dev:client": "npm run dev --workspace=client",
        "build": "npm run build --workspace=client && npm run build --workspace=server",
        "test": "npm run test --workspace=server && npm run test --workspace=client",
        "test:e2e": "playwright test",
        "test:e2e:ui": "playwright test --ui",
        "test:e2e:headed": "playwright test --headed",
        "test:e2e:debug": "playwright test --debug",
        "playwright:install": "playwright install",
        "lint": "npm run lint --workspace=server && npm run lint --workspace=client",
        "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
        "start": "npm run start --workspace=server",
        "docker:build": "docker build -t est:latest .",
        "docker:run": "docker run -d -p 3001:3001 -v est-data:/app/data --name est est:latest",
        "docker:stop": "docker stop est && docker rm est",
        "docker:logs": "docker logs -f est"
    },
    "devDependencies": {
        "@playwright/test": "^1.57.0",
        "@types/node": "^25.0.3",
        "@typescript-eslint/eslint-plugin": "^8.0.0",
        "@typescript-eslint/parser": "^8.0.0",
        "concurrently": "^9.2.1",
        "eslint": "^8.56.0",
        "prettier": "^3.2.0",
        "typescript": "^5.3.3"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=9.0.0"
    }
}
